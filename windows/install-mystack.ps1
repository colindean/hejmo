# Install my standard development languages and frameworks
# I'll probably never use this so it's probably out of date!
scoop install sbt scala terraform ngrok neovim netcat wireshark gradle

# install rustup to install rust instead of installing rust directly
scoop install rustup
rustup install stable
